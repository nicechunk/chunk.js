# Examples

Small examples should import from `../src/index.js` or `../../src/index.js` and avoid project-specific dependencies.

Current examples:

- `basic-voxel/`: starts the default WebGL2 voxel demo with the public engine API.
