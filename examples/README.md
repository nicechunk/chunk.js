# Examples

Small examples should import the narrow source module they exercise and avoid project-specific dependencies. Use the broad `src/index.js` barrel only when an example intentionally demonstrates the complete public graph.

Current examples:

- `basic-voxel/`: starts the default WebGL2 voxel demo with the public engine API.
